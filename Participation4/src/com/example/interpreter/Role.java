package src.com.example.interpreter;

/**
 * Simple enum for roles used in notifications/dashboards.
 */
public enum Role {
    EXEC, MANAGER, STAFF,
    DESIGN_LEAD, ENGINEERING_LEAD, FINANCE_LEAD, HR_LEAD
}
